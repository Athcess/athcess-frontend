import axios from "axios";

const getPost = aync () => {
    try {
        const res = await axios.get('');
    }catch(error) {
        console.log(error);
    }
}

const getProfilePic = asyn () => {
    try {
        const res = await axios.get('');
    }catch(error) {
        console.log(error);
    }
}