import axios from "axios";

const getPost = async () => {
    try {
        const res = await axios.get('');
    }catch(error) {
        console.log(error);
    }
}

const getProfilePic = async () => {
    try {
        const res = await axios.get('');
    }catch(error) {
        console.log(error);
    }
}